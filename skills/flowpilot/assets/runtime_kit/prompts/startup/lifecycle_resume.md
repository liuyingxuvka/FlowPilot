Resume the active FlowPilot run ${run_id} in ${project_root} by recording a manual lifecycle resume through `flowpilot_new.py resume --reason manual_resume`, then follow the returned lifecycle guard and foreground duty. Every resume must record the request before any wait or continuation claim. Do not self-classify the work chain as alive from stale role bindings, prior run state, stale runtime files, old Router state, chat history, or wait timeouts. The runtime must load the current run ledger, lifecycle guard, packet/lease state, and status projection before deciding whether to continue, wait, recover, block, or return terminal completion. Open, reuse, or replace only the currently requested responsibility named by the runtime; do not restore a fixed role set or prewarm all roles. A runtime-named reuse binding must return to its existing addressable AI execution surface; if that surface cannot be addressed after resume, treat it as role reattachment/recovery or blocker evidence unless the runtime explicitly authorizes replacement. Any new or replacement role binding must use a host-supported, isolated, addressable AI execution surface such as a background agent, separate thread, new conversation, worker, independent AI session, or equivalent host-supported mechanism, and must be explicitly requested with the strongest available host model and highest available reasoning effort; do not rely on foreground model inheritance. After lifecycle resume, process only the `flowpilot_new.py` foreground duty and stop only at a real role, user, host, payload, packet, blocker, or terminal-return boundary. Do not read sealed packet/result/report bodies or perform role work in the foreground.
