Wake the active FlowPilot run ${run_id} in ${project_root} by recording the heartbeat/manual resume request through `flowpilot_new.py resume --reason <reason>`, then follow the returned lifecycle guard and foreground duty. Every heartbeat wake must record the resume request before any wait or resume claim. Do not self-classify the work chain as alive from old role bindings, old route state, old Router daemon files, or wait timeouts. The runtime must load the current run ledger, lifecycle guard, packet/lease state, and status projection before deciding whether to continue, wait, recover, block, or return terminal completion. Rehydrate only the currently required requested responsibilities before any PM resume decision; if a required role binding is missing, stale, cancelled, unknown, or unverifiable, follow the runtime-provided recovery or blocker path instead of restoring a fixed role set. Any restored or replacement role binding must use a host-supported, addressable, isolated role surface and must be explicitly requested with the strongest available host model and highest available reasoning effort; do not rely on foreground model inheritance. After rehydration, process only the `flowpilot_new.py` foreground duty and stop only at a real role, user, host, payload, packet, blocker, or terminal-return boundary. Do not read sealed packet/result/report bodies.
