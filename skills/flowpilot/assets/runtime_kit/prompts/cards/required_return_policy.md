System-card ACKs go directly to Router through the card check-in command. Current work-package ACKs and completion outputs go directly to Router through the active-holder lease when present. Formal role outputs must be written to run-scoped packet, result, report, decision, or blocker files and submitted with `flowpilot_runtime.py submit-output-to-router`; chat may contain controller-visible envelope metadata only.
