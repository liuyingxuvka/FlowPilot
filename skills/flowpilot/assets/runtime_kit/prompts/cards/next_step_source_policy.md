Do not infer the next FlowPilot action from this card, chat history, or prior prompts. System-card ACKs, current work-package outputs, and formal role-output submissions go directly through the current runtime commands. Controller must follow the `flowpilot_new.py` lifecycle guard and foreground duty; old `flowpilot_router.py next` and `flowpilot_router.py run-until-wait` are old-run diagnostics or explicit unsupported-run repair tools only.
