# Route 001

This is the initial FlowPilot route for `<project-name>`.

## Nodes

1. `node-001-start`: complete material intake, local skill inventory,
   reviewer sufficiency, PM material understanding, PM research package
   closure when material gaps exist, and PM-owned product-function
   architecture; freeze the contract from those artifacts,
   write capability evidence, select child skills through the PM manifest, and
   verify the initial route.
2. `node-002-implement`: execute bounded implementation chunks with declared
   verification.
3. `node-003-verify`: run final verification and emit completion evidence.

Canonical source: `flow.json`.
