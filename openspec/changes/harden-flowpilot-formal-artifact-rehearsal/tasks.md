## 1. FlowGuard And Coverage Planning

- [x] 1.1 Confirm real FlowGuard/project audit, OpenSpec artifacts, and current worktree/peer-agent boundary before code edits.
- [x] 1.2 Identify the existing runtime/test owners for fake-AI rehearsal, formal FlowGuard evidence artifacts, PM repair packets, and break-glass retry counting.
- [x] 1.3 Add or update a focused FlowGuard/coverage model artifact that declares formal artifact lifecycle axes and retry oracles.

## 2. Fake-AI And Contract-Exhaustion Coverage

- [x] 2.1 Extend fake-AI responder behavior so formal artifacts are controlled by responder fault modes rather than silently helper-written.
- [x] 2.2 Add Cartesian cases for missing artifact, invalid JSON artifact, missing internal decision field, wrong enum value, wrong path/currentness, and body/artifact decision conflict.
- [x] 2.3 Add tests that fail when reissue feedback lacks artifact path, artifact field path, allowed values/type, or body-only insufficiency wording.
- [x] 2.4 Add tests that compare `packet_stage_evidence_matrix` required context fields against generated PM repair packets for FlowGuard failures.
- [x] 2.5 Add historical replay coverage for the WorldGuard body-plus-formal-artifact failure chain.

## 3. Runtime Minimal Repair

- [x] 3.1 Project existing FlowGuard formal evidence paths and failed-check summaries into PM repair packets without adding compatibility fields or fallback acceptance.
- [x] 3.2 Make formal artifact reissue packets executable by naming the artifact filename, target path/root, missing internal field, expected values/type, and body-only insufficiency.
- [x] 3.3 Count same-family mechanical formal-artifact failures toward the existing fifth-attempt break-glass threshold and reset the loop on current accepted repair.
- [x] 3.4 Preserve current-contract rejection of old paths, old runs, body-only substitutes, wrapper shapes, missing defaults, and aliases.

## 4. Validation And Synchronization

- [x] 4.1 Run focused new/changed tests for fake-AI coverage, runtime contract feedback, PM repair projection, historical replay, and break-glass threshold.
- [x] 4.2 Run affected FlowGuard model/test alignment or focused simulation checks and inspect final artifacts.
- [x] 4.3 Rebuild/check project topology if model, runner, test registry, prompt/card, or readiness surfaces changed.
- [x] 4.4 Run install self-checks and sync the local installed FlowPilot skill/runtime after tests pass.
- [x] 4.5 Update OpenSpec task checkboxes, FlowGuard adoption notes if needed, and local git state/version evidence without touching unrelated peer work.
