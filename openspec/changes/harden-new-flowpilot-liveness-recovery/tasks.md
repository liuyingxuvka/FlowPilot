## 1. Modeling And Contracts

- [x] 1.1 Update the lifecycle guard FlowGuard model for wait recovery ladder states, slow-live progress, accepted-packet reassignment, and original-result-wins race hazards.
- [x] 1.2 Update model-test alignment and known-friction notes so the new miss family is visible in validation output.

## 2. Runtime Implementation

- [x] 2.1 Add wait recovery metadata and thresholds to the new runtime lifecycle guard without reintroducing old daemon authority.
- [x] 2.2 Add hard gates that reject assignment or ACK state regression for packets with `accepted_result_id`.
- [x] 2.3 Add a public progress command that records lease progress and refreshes foreground duty without completing the packet.
- [x] 2.4 Add an accepted-packet race repair helper that closes/supersedes mistaken replacement leases and restores accepted packet status.

## 3. Active Run Repair

- [x] 3.1 Audit `run-20260530-102304` before repair and capture the packet/lease/result inconsistency.
- [x] 3.2 Repair the active run so `packet-0003` remains accepted on `result-0003`, `lease-0004` is no longer active, and `packet-0004` is the next actionable packet.

## 4. Tests And Rehearsals

- [x] 4.1 Add focused runtime/entrypoint tests for progress, slow wait grace, accepted assignment rejection, ACK regression rejection, and accepted-race repair.
- [x] 4.2 Add public fake AI rehearsal coverage for slow reviewer progress and accepted-result reassignment.
- [x] 4.3 Run focused tests and update checked-in result artifacts generated by those checks.

## 5. Validation And Sync

- [x] 5.1 Run OpenSpec validation, FlowGuard project audit, lifecycle/fake rehearsal/model checks, and targeted pytest suites.
- [x] 5.2 Start heavier model regressions in background where practical and inspect completion artifacts before claiming evidence.
- [x] 5.3 Sync the local installed `flowpilot` skill from the repository copy and run install checks.
- [x] 5.4 Review git status, avoid unrelated reversions, and commit or report the scoped local git state according to the dirty worktree boundary.
