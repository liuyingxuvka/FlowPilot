## 1. OpenSpec and FlowGuard Modeling

- [x] 1.1 Keep the new OpenSpec deltas scoped to existing wait reconciliation, partial-batch accounting, model-test alignment, router TestMesh, and known-friction gates.
- [x] 1.2 Add a focused FlowGuard packet-result family parity model using obligation-family parity and analogous-defect scan helpers.
- [x] 1.3 Feed packet-result family parity status into model-test alignment and broad coverage evidence so missing sibling evidence blocks full confidence.

## 2. Router Reconciliation

- [x] 2.1 Extend Router durable result reconciliation so `current_node` durable result envelopes fold missing worker-result evidence even when a family-level flag is already true.
- [x] 2.2 Extend PM role-work durable result reconciliation so every active batch member can be folded from durable result envelopes before waits/reminders are selected.
- [x] 2.3 Preserve existing material and research reconciliation behavior, sealed-body boundaries, and wrong-recipient rejection.

## 3. Runtime Test Evidence

- [x] 3.1 Add focused runtime tests for research full and partial durable-envelope reconciliation without manual worker return events.
- [x] 3.2 Add focused runtime tests for current-node full and mixed manual/durable batch reconciliation.
- [x] 3.3 Add focused runtime tests for PM role-work full and partial durable-envelope reconciliation.
- [x] 3.4 Add focused wrong-recipient and sealed-body provenance coverage for the packet-result family.

## 4. Validation and Sync

- [x] 4.1 Run focused router runtime tests that cover the packet-result family.
- [x] 4.2 Run the new packet-result FlowGuard model, model-test alignment, and control-plane state consistency checks.
- [x] 4.3 Run or launch documented heavier regression checks and inspect final artifacts before reporting them.
- [x] 4.4 Sync repository-owned FlowPilot assets into the local installed skill/runtime copy.
- [x] 4.5 Run install audit/checks serially after sync.

## 5. Finalization

- [x] 5.1 Mark OpenSpec tasks complete only after implementation and validation evidence exists.
- [x] 5.2 Review git status and preserve peer-agent changes; avoid staging or committing unrelated peer files.
- [x] 5.3 Record predictive-KB postflight if the work exposes a reusable lesson or route gap.
