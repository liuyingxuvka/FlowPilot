## 1. Model-Miss Grounding

- [x] 1.1 Record the observed WorldGuard-style model miss as derived FlowGuard reissue material inheritance loss.
- [x] 1.2 Inspect the current runtime reissue creation path and identify the owner code contract that must carry inherited reads into the envelope.
- [x] 1.3 Confirm existing FlowGuard model boundaries to extend rather than creating a parallel model.

## 2. Runtime Contract Repair

- [x] 2.1 Add a current-contract helper that derives inherited required authorized result reads for FlowGuard reissue packets from the blocked source packet.
- [x] 2.2 Pass inherited authorized reads into `issue_task_packet` when runtime creates a FlowGuard current-contract reissue packet.
- [x] 2.3 Preserve current new-only behavior and avoid compatibility aliases, fallback body guessing, or legacy translation.

## 3. FlowGuard Model Coverage

- [x] 3.1 Extend FieldLifecycleMesh with a derived material-read lifecycle chain from source FlowGuard packet to reissue packet envelope and handoff contract.
- [x] 3.2 Extend ContractExhaustionMesh with inherited-read-loss cases for FlowGuard reissue packets.
- [x] 3.3 Extend Cartesian control-plane exhaustion with mutation coverage for derived material inheritance loss.
- [x] 3.4 Extend synthetic-agent coverage so fake AI/replay rows consume the derived inheritance bad cases.
- [x] 3.5 Extend Model-Test Alignment obligations, owner code contracts, and test evidence rows for `flowguard_reissue_preserves_required_authorized_result_reads`.
- [x] 3.6 Extend coverage inventory/topology inputs if model runner or evidence surfaces change.

## 4. Tests

- [x] 4.1 Add focused runtime regression for the WorldGuard path: missing `semantic_recheck` causes reissue and the reissue inherits required reads.
- [x] 4.2 Add negative runtime regression proving a reissued FlowGuard result cannot submit before inherited required result bodies are opened.
- [x] 4.3 Add model tests for FieldLifecycleMesh, ContractExhaustionMesh, Cartesian, Model-Test Alignment, and synthetic coverage rows.
- [x] 4.4 Add or update fake-agent/replay coverage where needed so same-class derived inheritance cases are not point-only tests.

## 5. Validation And Sync

- [x] 5.1 Run OpenSpec strict validation for this change.
- [x] 5.2 Run focused runtime and model tests affected by the repair.
- [x] 5.3 Run FlowGuard model runners for field contract, contract exhaustion, Cartesian, Model-Test Alignment, synthetic coverage, and coverage inventory.
- [x] 5.4 Run meta/capability checks through the repository background artifact contract if required by touched surfaces.
- [x] 5.5 Rebuild and check FlowGuard project topology.
- [x] 5.6 Sync and verify the local installed FlowPilot skill.
- [x] 5.7 Record FlowGuard adoption evidence and KB postflight observation.
