## 1. Baseline, Ownership, And Evidence Honesty

- [x] 1.1 Upgrade the repository FlowGuard project record from installed 0.53.0 evidence to the current 0.53.1 engine and retain the deterministic topology schema upgrade.
- [x] 1.2 Produce a full ExistingModelPreflight snapshot covering packet/result contracts, current handoff/checklist, fake-AI replay, current Cartesian, TestMesh, MTA, ModelMesh, and release owners.
- [x] 1.3 Register the externally visible commitments and single primary owner models in the Behavior Commitment Ledger; mark path-sensitive single-authority commitments for PPA evidence.
- [x] 1.4 Inventory predecessor Cartesian/fake-AI OpenSpec changes and record retain, merge, supersede, or archive-history disposition without rewriting old verification history.
- [x] 1.5 Add red tests that expose body-over-envelope authority, zero forbidden projection, static review-policy reconstruction, generic family fallback, wrong-role continuation, daemon replay continuation, and hard-coded evidence green.
- [x] 1.6 Change coverage summaries to report declared, applicable, excluded, generated, selected, executed, passed, failed, stale, and proof-backed counts separately.
- [x] 1.7 Extend ExistingModelPreflight and the predecessor disposition table for requested-role resume, role-continuity memory, compact Reviewer contract, stage-specific challenge, review-window coverage, and earlier Cartesian/formal-artifact evidence; classify each as retain, merge, supersede, archive-history, or block without treating checked tasks as current proof.

## 2. Single Contract Authority And Field Lifecycle

- [x] 2.1 Complete `current_handoff_contract.required_report_contract` projection for required/child/array/non-empty/branch/options/types/forbidden fields and aliases, current profile bindings, and dynamic owner/id rules already owned by the effective contract.
- [x] 2.2 Record FieldLifecycleMesh rows and old-path disposition for packet-body mechanical mirrors, conditional body fields, private helper shapes, role aliases, and daemon execution source.
- [x] 2.3 Rewrite submission-checklist creation as a pure envelope-handoff projection with current run/packet/lease/route/source-generation identity and deterministic contract fingerprint.
- [x] 2.4 Make incomplete, wrong-schema, stale, or conflicting handoff/checklist data fail closed with current repair feedback; do not repair from body or remembered registry values.
- [x] 2.5 Remove role-handoff prompt text that permits packet-body `minimal_valid_shape`; instruct roles to use only the returned submission checklist.
- [x] 2.6 Update current prompt/card/contract-index surfaces so they describe the single checklist authority and do not reintroduce forbidden/deleted positive fields.
- [x] 2.7 Run deleted-field/alias/body-fallback residue search and classify every remaining hit as current, forbidden negative, or historical-only evidence.
- [x] 2.8 Normalize structured current-authority items inside the existing handoff and `node_context_package.relevant_references` surfaces, binding reference kind, authority id, owner, scoped path, fingerprint, consumer scope, and every applicable current runtime identity; reject missing, stale, conflicting, prose, history, newest-artifact, compatibility, translation, alias, and fallback authority without adding a second ledger or top-level result field.

## 3. Canonical Fake-AI And Review Execution

- [x] 3.1 Replace body-derived fake-AI construction with a strict `from_open_packet_result` path that validates ACK, run, packet, lease, route/source generation, checklist schema/fingerprint, and required material delivery.
- [x] 3.2 Project forbidden fields and aliases from the checklist and add a dynamic non-zero parity gate from registry through execution; the current baseline must close all 13 base families and 122 registered forbidden fields.
- [x] 3.3 Make Reviewer fake AI consume and echo the delivered `review_window.review_depth_rule`; missing, unknown, or tampered policy must fail closed.
- [x] 3.4 Remove `from_packet`, arbitrary reissue-body, static review-policy reconstruction, generic result, generic route-plan, and unknown-family success fallbacks from active rehearsal paths.
- [x] 3.5 Refactor fake-project rehearsal to preserve the real open-packet result/checklist and generate every submitted payload from that current result.
- [x] 3.6 Refactor core fake E2E to stop using private runtime minimal-shape helpers and execute the public dispatch/ACK/open/checklist/submit path.
- [x] 3.7 Add negative tests for no ACK/open, stale checklist after reissue, unopened required reads, cross-run replay, wrong packet/lease/role, missing family, and body/checklist conflict.
- [x] 3.8 Preserve the existing `Workstream Plan and Completion` subsection for every substantive role, keep Runtime limited to the current mechanical contract, and make Reviewer compare the rows with structured current-authority references, real artifacts, delegation integration, verification, unresolved items, and claim consistency.
- [x] 3.9 Reconcile Reviewer prompts, fake AI, model invariants, MTA rows, and tests with the compact current result contract; retain independent challenge as mandatory behavior, keep `independent_challenge` deleted/forbidden as a field, and classify shallow or generic optimization-only positive fixtures as known-bad without adding a production natural-language judge.

## 4. Current Responsibility And Execution-Source Purity

- [x] 4.1 Remove or reject unsupported responsibility aliases and unknown-role title/default handling; keep result-body reviewer role labels separate from packet responsibility enums.
- [x] 4.2 Resolve `planner` and `flowguard_operator` node responsibility ownership explicitly; no unsupported responsibility may normalize silently to Worker.
- [x] 4.3 Remove `daemon_replay` from positive current stage profiles and make it a historical negative with an explicit mechanical oracle.
- [x] 4.4 Make `wrong_role`, retired aliases, stale workspace, old router, body-only contract, and private-helper source cells reject rather than continue.
- [x] 4.5 Add current-role/source negative tests across route planning, packet dispatch, open, submit, replay, and current-contract Cartesian expectations.
- [x] 4.6 Make public resume and the retained diagnostic router derive the same exact deduplicated target set from current unresolved packet/Controller-wait owners plus the immediate foreground-duty role; exclude idle same-run slots, fixed role sets, history/chat/prior-run roles, duplicate targets, and PM when no current PM decision is required.
- [x] 4.7 Split MTA resume ownership into public requested-role reentry and retained diagnostic obligation replay, bind both to current CodeContracts and external tests, and prevent internal router receipts from substituting for public `flowpilot_new.py resume` plus `dispatch-current-role` evidence.

## 5. Contract Coverage Universe And Oracles

- [x] 5.1 Add a formal AI-response `ContractCoverageUniverse` with finite dimensions, interaction groups, payload boundaries, claim scope, and explicit exclusions.
- [x] 5.2 Add `ContractOracle` rows with expected status, error/feedback fields, repair fields, forbidden downstream action, state transition, side-effect boundary, and next action.
- [x] 5.3 Regenerate stable case ids from full source contract paths and add a hard failure for all current/future id collisions, including the 22 audited collisions.
- [x] 5.4 Define and test `oracle_signature` equivalence; require oracle/owner/feedback/transition/side-effect equality before many-to-one canonical compression.
- [x] 5.5 Add `ObservedProblemBackfeed` for the DataBank miss, zero-forbidden coverage, checklist bypass, static Reviewer policy, wrong-role, daemon replay, and every new public-runtime discrepancy.
- [x] 5.6 Update ContractExhaustionMesh shards/receipts and remove duplicate hand-written positive generators that would remain alternate authorities.
- [x] 5.7 Pin known-bad cases for idle/all-slot/fixed-role resume, omitted current obligation roles, stale role ids, missing or conflicting authority references, deleted Reviewer fields, shallow/generic review, missing or contradictory workstream evidence, live descendants after interruption, and mixed-fingerprint receipt consumption.

## 6. Executable Coverage Harness

- [x] 6.1 Implement the canonical formal AI execution harness and `tests.test_flowpilot_formal_ai_contract_execution` required by the verification contract.
- [x] 6.2 Execute every single required/child/array/non-empty/type/enum/forbidden/alias/identity mutation through the real validator with exact error, feedback, reissue, and no-forbidden-side-effect assertions.
- [x] 6.3 Generate a constraint-aware pairwise covering array and execute every selected row through dispatch, lease, ACK, open-packet, checklist response, and public submit-result.
- [x] 6.4 Add high-risk three-wise groups for identity/timing/route, decision/branch/owner, review-policy/material, accepted-pointer/closure, and repair/retry/no-delta interactions.
- [x] 6.5 Pin and execute all historical miss fixtures regardless of covering-array selection.
- [x] 6.6 Add deterministic fuzz profiles for duplicate keys, invalid numeric values, BOM/encoding, top-level non-object, prose/Markdown wrappers, excessive size/depth, replay, concurrent double submit, and cross-run isolation.
- [x] 6.7 Benchmark 30-80 public-path cases, record per-case and shard cost, and use the measurement to set final deterministic shard sizes without dropping required coverage.

## 7. Proof-Backed FlowGuard Evidence Mesh

- [x] 7.1 Add `flowpilot_ai_response_execution_closure_model.py`, its runner, result artifact, and known-bad tests for universe/oracle/receipt/evidence-truth hazards.
- [x] 7.2 Replace hard-coded TestMesh `passed/current/abstract_green` child rows with real command/result/exit/fingerprint/ProofArtifactRef evidence and current reuse tickets where justified.
- [x] 7.3 Make TestMesh actual `test_count` equal selected/executed tests, not owned model cells; bind every required case/shard id to one current child owner.
- [x] 7.4 Split coarse MTA Cartesian obligations by risk shard and bind owner CodeContracts, current external tests, source audits, and public-runtime/boundary observations.
- [x] 7.5 Remove any layered proof path that copies expected owner/reaction/status into observed fields; observed values must come from current execution artifacts.
- [x] 7.6 Add current child `ModelContractCoverageReceipt`s and `CompositeHandoffAcceptance`; make ModelMesh parent consume every required receipt and affected sibling disposition.
- [x] 7.7 Add missing/stale/progress-only/failed/reused-without-ticket known-bad evidence variants and prove they block parent, done, release, and publish confidence.
- [x] 7.8 Rebuild BCL/DCAR/MTA/TestMesh/ModelMesh/Risk evidence handoffs and record exact evidence/freshness ids in DevelopmentProcessFlow.
- [x] 7.9 Add current owner/evidence rows for structured authority references, exact requested-role resume, compact Reviewer semantics, workstream semantic audit, descendant-zero cleanup, and same-fingerprint manifest consumption; every protected failure needs one good case, one known-bad case, an oracle, and current evidence.

## 8. Test Tiers, Background Execution, And Performance

- [x] 8.1 Add explicit `formal-submit-fast` and `formal-submit-adversarial` TierCommand groups and register them in tier names, `all`, release, and final-confidence ownership as specified.
- [x] 8.2 Put authority/cardinality, all single faults, focused contract exhaustion, public pairwise, and affected fast shards in the PR lane.
- [x] 8.3 Put full structural enumeration, fake-AI replay, current-contract matrix, pairwise full set, historical misses, deterministic fuzz, and high-risk interactions in the adversarial/nightly lane.
- [x] 8.4 Make publish closure consume adversarial, router, integration, the release tier's Meta/Capability and public-release evidence, plus the later final-confidence receipt; `all` alone is not release proof.
- [x] 8.5 Use deterministic shards, compact aggregate results, bounded failure samples, fail/continue policy, and isolated temporary roots; default Windows background parallelism to two after the benchmark.
- [x] 8.6 Add tier tests proving command ownership, no hidden skip, final background artifact requirements, timeout/failure propagation, and budget reporting.
- [x] 8.7 Bind every heavyweight launcher to exactly one execution owner and process-tree identity; after timeout, cancellation, interruption, or supervisor failure, invalidate partial evidence and prove descendant count zero before reuse or another owner, with `cleanup-unconfirmed` blocking Scheduled Task, unattended retry, background resume, and parent-exit-only shortcuts.
- [x] 8.8 Freeze one covered-source fingerprint across all/adversarial/release supervisors and children, the compiled manifest, ContractExhaustion/current Cartesian/MTA/TestMesh/ModelMesh consumers, final-confidence, Meta/Capability parents, topology, install, and final provider validation; reject mixed, newest, approximate, or command-text-equivalent receipts.
- [x] 8.9 Replace exact global-fingerprint applicability with one canonical LF snapshot fingerprint for provenance plus one MTA/TestMesh-derived covered-input identity per execution owner; do not add a parallel manual owner table.
- [x] 8.10 Add one impact resolver that emits exactly `reuse`, `execute`, or `blocked`; map affected owners and declared dependents, map explicit shared-global inputs to one declared full owner, and block unmapped or ambiguous inputs as `impact_mapping_missing` without `run all`.
- [x] 8.11 Persist owner covered-input, command, dependency, toolchain, environment, terminal-result, and cleanup identities in execution proof and issue a real current `TestResultReuseTicket` for unchanged owners.
- [x] 8.12 Make foreground and background tier entrypoints consume the frozen impact plan, execute only selected owners, preserve unaffected artifacts, and keep receipt consumers read-only.
- [x] 8.13 Remove unconditional Meta/Capability `--force`; verification and release consume their current owner receipts unless the impact plan explicitly selects those heavyweight owners.
- [x] 8.14 Keep the canonical snapshot fingerprint as provenance and mixed-snapshot protection only; delete the former global-mismatch-to-all-stale success path and add a negative test that forbids its return.
- [x] 8.15 Make graph-generated supplemental MTA owners distinguish exact test functions from module/class/fixture evidence, use the existing declared command for non-function evidence, and run only after ordinary upstream owners complete.
- [x] 8.16 Make terminal metadata publish atomically before the atomic exit marker, and add a regression proving the supervisor cannot observe exit with stale running metadata.
- [x] 8.17 Separate shared execution-wrapper imports from nested payload applicability, bind them to the current test-tier infrastructure owner, and permit former broad payload proof reuse only through an exact proper-subset ownership-transfer proof.

## 9. Focused Verification And Failure Backfeed

- [x] 9.1 Run authority projection, new entrypoint, canonical fake-AI execution, fake project, and focused runtime tests; classify and repair every failure.
- [x] 9.2 Run the new execution-closure, contract-exhaustion, fake-AI replay, current-contract Cartesian, MTA, TestMesh, ModelMesh, and layered-boundary checks.
- [x] 9.3 For any runtime/test discrepancy after a model pass, run Model-Miss Review and update owner commitment, same-class cases, code/test binding, shards, and receipts before continuing.
- [x] 9.4 Rebuild and check FlowGuard project topology after all model, runner, test, prompt/card, contract, and tier changes.
- [x] 9.5 After its owning workflow synchronizes the existing verification contract with these new obligations, run that contract and keep the change open until every required check is current, terminal, same-fingerprint, and passing.
- [x] 9.6 Run focused positive and known-bad tests for authority references, requested-role target equality, compact Reviewer/deleted-field behavior, workstream semantic audit, descendant-zero cleanup, and fingerprint invalidation; backfeed every discrepancy before broad execution.
- [x] 9.7 Add focused tests for CRLF/LF equivalence, real-content drift, unrelated-owner reuse, exact affected-owner invalidation, declared shared-global ownership, unmapped blocking, real reuse-ticket consumption, no newest/fallback path, and receipt-consumer non-execution.

## 10. Background Regression Closure

- [x] 10.1 Freeze source, toolchain, test inventory, dependencies, execution owners, verification plan, and one covered-source fingerprint; run `all`, `formal-submit-adversarial`, and `release` in isolated roots with matching start/end fingerprints, final per-child artifacts, and bounded concurrency.
- [x] 10.2 Compile the final acceptance evidence manifest only from those same-fingerprint terminal tiers; then run ContractExhaustion, current Cartesian, MTA, acceptance TestMesh, and ModelMesh against that exact manifest before starting `final-confidence`.
- [x] 10.3 Run Meta and Capability full regressions exactly once inside the release tier through the repository background-log contract; verify out/err/combined/exit/meta artifacts, final status, freshness, and any proof reuse, then let later final-confidence and parent consumers inspect those receipts without relaunching either owner.
- [x] 10.4 Inspect every supervisor/child final exit, result, metadata, fingerprint, and descendant-zero artifact; running, progress-only, stale, skipped, timeout, failed, mixed-fingerprint, or cleanup-unconfirmed evidence remains non-passing.
- [x] 10.5 Fix all regression failures at their owning code/model/test route and rerun only the invalidated scope plus mandatory release parents.
- [x] 10.6 Audit the final dependency graph to prove every receipt consumer used the frozen manifest/fingerprint and no receipt consumer reran a heavy owner, selected a newest result, or crossed an invalidated evidence boundary.
- [x] 10.7 Update the tiering, acceptance TestMesh, MTA, BCL, ModelMesh, and DevelopmentProcessFlow evidence models with the canonical impact-plan states and a known-bad blanket-rerun hazard.
- [x] 10.8 Run minimum affected validation first, reuse every unchanged current proof, and launch only impact-selected model or test owners in the repository background-log contract.
- [x] 10.9 After source and the impact plan are stable, run the single declared full release gate, inspect all executed/reused/blocked rows, and prove later tool or unrelated FlowGuard changes do not mechanically invalidate unchanged FlowPilot behavior evidence.

## 11. Installation, Version, Documentation, And Public Boundary

- [x] 11.1 After source/model/test/evidence freeze, rebuild and check topology, then sync repository-owned FlowPilot into the local Codex skill installation.
- [x] 11.2 Run install sync, consumer-manifest-aware local digest audit, install check, and installed runtime self-check serially; prove source freshness and matching governed content digests without treating source-only files or the installed consumer manifest as drift.
- [x] 11.3 Update VERSION, CHANGELOG, README English/Chinese release statements, install/version examples, and release notes for v0.12.1 without overstating arbitrary AI correctness.
- [x] 11.4 Run SkillGuard contract/depth/release-facing checks, privacy scans, URL validation, and public-boundary checks; remove or exclude private/local evidence from the release.
- [x] 11.5 Complete the predecessor change disposition table and finish the retained ledger-hygiene KB/report tasks without using its old coverage report as successor proof.
- [x] 11.6 Through the workflow that owns the existing `verification-contract.yaml`, add obligations/check owners/dependencies/freshness inputs for structured authority references, exact requested-role resume, compact Reviewer/workstream semantics, descendant-zero cleanup, same-fingerprint sequencing, and predecessor disposition; this update-change task MUST NOT edit that file because OpenSpec status did not return it in `existingOutputPaths`.
- [x] 11.7 Update AGENTS, verification guidance, HANDOFF, version, changelog, and English/Chinese release statements so future AI treats a raw repository fingerprint mismatch as an impact-audit trigger, never as authority for a heavyweight blanket rerun.
- [x] 11.8 Through the separate verification-contract owner workflow, replace mandatory all/adversarial/release and Meta/Capability force execution with canonical impact-plan, reuse-ticket, selective-execution, unmapped-block, and cheap parent-consumer checks.

## 12. OpenSpec, Local Handoff, And Final Audit

- [x] 12.1 Run `openspec validate restore-flowpilot-test-evidence-closure --type change --strict --no-interactive`, inspect every reported requirement/scenario, and archive only after strict validation and every separately owned verification-contract check are current.
- [x] 12.2 Inspect the full diff and working tree, preserve unrelated untracked/user files, stage only task-owned paths, create one local Git commit, and do not stage or commit concurrent/user-owned work.
- [x] 12.3 Record the latest superseding 2026-07-19 user scope: one exact task-owned local commit, task-branch push, fast-forward default-branch update, annotated `v0.12.1` tag, and source-only GitHub Release are required.
- [x] 12.4 After all local evidence is current, re-fetch the remote, reject branch/tag/release collisions, push the task branch, fast-forward the default branch, push the annotated tag, create the GitHub Release, and verify every remote identity resolves to the exact task commit.
- [x] 12.5 Run a final DevelopmentProcessFlow/Risk Evidence Ledger audit covering every requirement, skipped check, residual risk, install sync, local and remote Git state, publication evidence, and claim boundary.
- [x] 12.6 Run predictive KB postflight and record mistake-first, FlowGuard/OpenSpec, delegation, testing, install-sync, and release lessons that are reusable.
- [x] 12.7 Mark the active user goal complete only after every in-scope local and remote action-list row has authoritative current evidence.
