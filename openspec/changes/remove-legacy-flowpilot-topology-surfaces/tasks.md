## 1. OpenSpec And Inventory

- [x] 1.1 Validate this OpenSpec change and use it as the implementation boundary.
- [x] 1.2 Update completed-but-active OpenSpec change artifacts that still describe fixed six-role, Process/Product-scope FlowGuard operator, Validator, Closure Officer, or old Router daemon behavior as current authority.
- [x] 1.3 Run a focused inventory over active source, templates, docs, simulations, and installed skill text for legacy topology terms.

## 2. Prompt, Template, And Documentation Cleanup

- [x] 2.1 Update `skills/flowpilot/SKILL.md` and active runtime kit prompts/cards so fresh runtime guidance names `flowpilot_new.py`, lifecycle guard duty, and current requested responsibilities only.
- [x] 2.2 Update `templates/flowpilot` README, role binding, capability, continuation, heartbeat, and packet templates to remove fixed six-role/runtime roles/sidecar wording as current requirements.
- [x] 2.3 Update public README, protocol, schema, verification, HANDOFF, and reference docs so current guidance no longer teaches old topology or old Router authority.

## 3. Runtime, Tests, And Models

- [x] 3.1 Remove Process/Product-scope FlowGuard operator aliases from current runtime responsibility menus, host aliases, packet issuance, and tests; use only the explicit `flowguard_operator` responsibility.
- [x] 3.2 Update simulations and model-test alignment source obligations so validation and closure are system outcomes and requested responsibilities replace fixed six-role reports.
- [x] 3.3 Regenerate focused result artifacts for touched model/test boundaries.

## 4. Validation, Install Sync, And Git

- [x] 4.1 Run OpenSpec validation and focused unit/model checks for prompt boundary, new entrypoint/core runtime, requested-role binding, resume, and install checks.
- [x] 4.2 Run required meta/capability regressions in the background log contract and inspect final artifacts.
- [x] 4.3 Rebuild and check FlowGuard project topology after generated artifacts change.
- [x] 4.4 Sync the installed FlowPilot skill and verify install freshness.
- [x] 4.5 Stage and commit only this cleanup, preserving unrelated existing worktree changes.
