## 1. Preflight And OpenSpec

- [x] 1.1 Record FlowGuard package/version/project-audit evidence and current dirty-worktree/peer-agent boundaries.
- [x] 1.2 Validate the OpenSpec change and keep `reduce-flowpilot-contract-surface` as a sibling dependency, not an edited target.
- [x] 1.3 Record DevelopmentProcessFlow modes and minimum revalidation scope for model, test, install, and sync work.

## 2. Executable Bridge Model

- [x] 2.1 Add a FlowGuard-backed executable matrix bridge model with explicit row fields for model cell, fake body, Runtime/CLI entrypoints, event evidence, convergence, break-glass expectation, command evidence, and freshness.
- [x] 2.2 Add required bridge rows for current known miss families from fake project rehearsal and model-miss review.
- [x] 2.3 Add bridge invariants that reject model-only evidence for executable confidence, missing row ownership, stale receipts, and wrong break-glass expectations.
- [x] 2.4 Add a checker that writes a JSON result artifact with row counts, evidence-level counts, missing rows, stale rows, break-glass rows, and TestMesh child ids.

## 3. Fake Body And Runtime Evidence Binding

- [x] 3.1 Bind required bridge rows to existing fake project body generators and identify rows that only have body-level evidence.
- [x] 3.2 Bind public Runtime/CLI replay rows to existing fake project or real-router rehearsal tests where current evidence is strong enough.
- [x] 3.3 Add stale-result detection for bridge rows by comparing result artifacts with source model, test, fake body generator, and runtime entrypoint mtimes.
- [x] 3.4 Keep live AI semantic quality and product completion claims explicitly out of scope for bridge evidence.

## 4. Long-Chain Convergence And Break-Glass

- [x] 4.1 Add ordinary convergence rows proving attempts one through four of known recoverable repeated errors stay in normal control-plane recovery.
- [x] 4.2 Add the fifth same-class no-progress row proving Controller break-glass is required as a safety fuse.
- [x] 4.3 Add checks that a fifth-repeat break-glass pass is not counted as ordinary repair convergence.
- [x] 4.4 Add checks that any non-threshold break-glass in known recoverable rows fails the bridge.

## 5. Test And Coverage Integration

- [x] 5.1 Add unit tests for bridge row completeness, required miss family coverage, evidence-level classification, stale receipt rejection, and break-glass threshold semantics.
- [x] 5.2 Extend synthetic coverage or inventory reporting to include bridge summary without treating model-only rows as executable pass.
- [x] 5.3 Extend model-test alignment or coverage evidence rows so bridge child ids are visible to parent validation.
- [x] 5.4 Add focused tests proving existing fake project rehearsal rows can be consumed only when they cite bridge evidence and current receipts.

## 6. Validation, Install, And Sync

- [x] 6.1 Run focused bridge, current-contract matrix, fake project rehearsal, high-standard, core runtime, and control-plane tests.
- [x] 6.2 Run affected FlowGuard model checks and model-test/coverage inventory checks.
- [x] 6.3 Rebuild and check FlowGuard project topology after model/test/evidence changes.
- [x] 6.4 Sync repo-owned FlowPilot artifacts to the local installed skill and run install sync audits.
- [x] 6.5 Run install self-check and smoke validation; keep any unrelated peer-agent failures visible instead of hiding them.
- [x] 6.6 Inspect final diff/status without reverting peer changes and record KB postflight.
