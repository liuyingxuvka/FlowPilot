## 1. OpenSpec And FlowGuard Setup

- [x] 1.1 Validate the OpenSpec change artifacts.
- [x] 1.2 Confirm the real FlowGuard package, package version, and project audit before production edits.

## 2. Cartesian Matrix Model

- [x] 2.1 Add the control-plane boundary inventory, mutation alphabet, handoff contexts, downstream consumers, and recovery reactions.
- [x] 2.2 Generate the full Cartesian product with explicit skip reasons for inapplicable combinations.
- [x] 2.3 Attach a current-contract oracle, repair command, subject, owner, evidence owner, and GlassBreak policy to every applicable cell.
- [x] 2.4 Add FlowGuard invariants and hazard states for missing oracle, missing owner, silent skip, normal GlassBreak pass, unsupported fallback acceptance, and no-delta retry.

## 3. Runner And Evidence

- [x] 3.1 Add a runner that writes a deterministic JSON result artifact.
- [x] 3.2 Add TestMesh child-suite summaries for every generated evidence owner.
- [x] 3.3 Add bridge evidence proving existing contract-exhaustion and historical failure inputs are consumed by the Cartesian layer.

## 4. Registry Integration

- [x] 4.1 Register the Cartesian runner in coverage sweep and fast test tiers.
- [x] 4.2 Register Cartesian model/runner/replay obligations in Model-Test Alignment.
- [x] 4.3 Register Cartesian obligations in synthetic-agent coverage.
- [x] 4.4 Register the Cartesian leaf matrix in layered boundary proof and coverage inventory.

## 5. Tests

- [x] 5.1 Add tests for full product counts, skip reasons, required high-risk mutations, and owner coverage.
- [x] 5.2 Add tests that normal repair cells never accept GlassBreak.
- [x] 5.3 Add tests that GlassBreak threshold cells require repeated blocker keys.
- [x] 5.4 Add tests that unsupported compatibility/fallback surfaces are rejected, not translated.
- [x] 5.5 Add integration tests for MTA, synthetic coverage, layered proof, inventory, and test tiers.

## 6. Validation And Fixes

- [x] 6.1 Run OpenSpec validation for the change.
- [x] 6.2 Run the Cartesian runner and targeted pytest suite.
- [x] 6.3 Fix any bugs exposed by the matrix or tests without adding compatibility/fallback surfaces.
- [x] 6.4 Rebuild and check the FlowGuard topology.

## 7. Sync And Postflight

- [x] 7.1 Sync installed/local package version evidence, repository evidence, Gate/test-tier evidence, and FlowGuard adoption logs.
- [x] 7.2 Perform predictive KB postflight and record a structured observation if this work exposes a reusable miss or route gap.
