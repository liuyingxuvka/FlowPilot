## 1. Scope, Baseline, And OpenSpec

- [x] 1.1 Run predictive KB preflight, coordination checks, real FlowGuard
  import preflight, current git scope review, OpenSpec list/status, maintenance
  map, coverage sweep, and full coverage inventory.
- [x] 1.2 Validate this OpenSpec change strictly and fix spec/task issues before
  behavior-bearing edits.
- [x] 1.3 Record a baseline residual table covering protocol/source, terminal,
  live runtime, final confidence, model mesh, test mesh, and structure mesh
  groups.

## 2. Protocol Contract Conformance

- [x] 2.1 Split current protocol conformance source failures by contract family
  and map each family to model obligation, source owner, public boundary, and
  focused tests.
- [x] 2.2 Repair startup reviewer fact, control blocker, PM resume decision,
  role-output loader, material scan packet-body, wait producer, and
  process/result binding failures until protocol conformance is green or any
  remaining item is explicitly blocked with evidence.
- [x] 2.3 Refresh protocol conformance result artifacts and focused tests.

## 3. Terminal And Live Runtime Closure

- [x] 3.1 Repair terminal monotonicity source/code findings and refresh terminal
  result artifacts.
- [x] 3.2 Repair or explicitly dispose live packet-authority projection and host
  automation cleanup findings without destructive runtime cleanup.
- [x] 3.3 Re-run affected control-plane friction, model mesh, terminal, and
  final-confidence checks.

## 4. Model/Test Mesh And Evidence Classification

- [x] 4.1 Classify unclassified model tiers into supporting, coverage-strong,
  specialized local hazard, ModelMesh, or TestMesh owners.
- [x] 4.2 Refresh model-test alignment, model maturation, model hierarchy, full
  coverage inventory, and final confidence evidence after repairs.
- [x] 4.3 Ensure stale, skipped, progress-only, or release-only evidence remains
  blocking or explicitly scoped.

## 5. Structure And Test Compression

- [x] 5.1 Run Architecture Reduction over remaining runtime-owner and script
  hotspots and assign proof status to each candidate.
- [x] 5.2 Apply StructureMesh only to safe-by-facade or safe-by-equivalence
  candidates, preserving public imports, commands, result paths, and JSON
  shapes.
- [x] 5.3 Apply TestMesh to large or layered tests by external contract family
  before moving internal fixtures.
- [x] 5.4 Defer unsupported compression candidates with a watchlist entry
  instead of claiming them complete.

## 6. Background Regression And Final Evidence

- [x] 6.1 Launch heavyweight Meta and Capability model checks through the stable
  background artifact contract and inspect stdout, stderr, combined, exit, meta,
  timestamp, and proof-reuse status.
- [x] 6.2 Run required router, integration, final-confidence, coverage sweep,
  full coverage inventory, and public-boundary/privacy checks for touched
  surfaces.
- [x] 6.3 Reach final evidence where `runner_not_ok` is zero or every remaining
  item is explicitly blocked/out-of-scope without overclaiming final
  convergence.

## 7. Install Sync, Logs, And Local Git

- [x] 7.1 Sync repo-owned FlowPilot into the installed local skill, then run
  install audit, install check, and `check_install.py` in sequence.
- [x] 7.2 Update adoption logs, handoff/maintenance notes, OpenSpec task state,
  and KB postflight.
- [x] 7.3 Stage intended files only, create a local git commit on `main`, and
  report the commit id without pushing, tagging, deploying, or releasing.
