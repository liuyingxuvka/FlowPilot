## 1. Contract And Model

- [x] 1.1 Add OpenSpec deltas for Router/Controller reconciliation, stateful postconditions, duplicate dispatch, current-scope pre-review, and startup display surface.
- [x] 1.2 Preserve and run the focused FlowGuard current-scope reconciliation model with stateful postcondition drift hazards.
- [x] 1.3 Record FlowGuard adoption evidence for the trigger, model files, commands, findings, skipped checks, and residual risk.

## 2. Runtime Reconciliation

- [x] 2.1 Make scheduled Controller receipt reconciliation replay or reclaim registered stateful postconditions when an already-reconciled row's Router-owned flag is false.
- [x] 2.2 Ensure invalid or missing postcondition evidence routes to bounded Controller repair/control-blocker evidence instead of ordinary duplicate dispatch.
- [x] 2.3 Keep startup display status using the generic stateful postcondition path and avoid hardcoded action-only fixes.

## 3. Duplicate Dispatch Guard

- [x] 3.1 Prevent next-action selection from issuing an equivalent Controller command while one is pending, running, waiting, done/reconciled, or drift-reconcilable.
- [x] 3.2 Verify independent nonblocking startup actions can still be queued when they are genuinely different and safe.

## 4. Runtime Tests And Verification

- [x] 4.1 Add targeted router runtime coverage for `write_display_surface_status` done/reconciled while `startup_display_status_written=false`.
- [x] 4.2 Add coverage that the same stateful action is not repeatedly requeued after reconciled postcondition drift.
- [x] 4.3 Run focused FlowGuard checks, targeted unit tests, and install checks.
- [x] 4.4 Start heavy meta/capability regressions through the background log contract and inspect final artifacts before reporting completion.

## 5. Sync And Git State

- [x] 5.1 Sync the validated repository-owned FlowPilot skill into the local installed skill.
- [x] 5.2 Run local install audit/check and verify installed source freshness.
- [x] 5.3 Review git state, preserve peer-agent changes, and report exactly what was changed locally.
