## 1. Preflight And Modeling

- [x] 1.1 Verify FlowGuard importability, current worktree status, and current OpenSpec state.
- [x] 1.2 Record the FlowGuard route: existing-model preflight, architecture reduction, model-test alignment, and development-process validation.
- [x] 1.3 Classify old terms into rejected-test evidence, stale active specs, stale result artifacts, no-op hooks, current recovery mechanisms, or historical docs.

## 2. OpenSpec And Prompt Cleanup

- [x] 2.1 Update active OpenSpec specs so retired inputs are rejected or audit-only, not accepted aliases.
- [x] 2.2 Add the validation-artifact canonicalization contract.
- [x] 2.3 Compress long runtime cards while preserving required role, runtime, FlowGuard, recovery, and sealed-body obligations.
- [x] 2.4 Validate prompt/card coverage and current-only prompt boundaries.

## 3. Code And Evidence Cleanup

- [x] 3.1 Remove empty gate-contract alias lookup and update public exports/imports.
- [x] 3.2 Remove no-op packet envelope alias normalization and update import sites.
- [x] 3.3 Prefer canonical `*_results.json` artifacts in thin-parent and model-hierarchy evidence selection.
- [x] 3.4 Remove stale shadow result artifacts only after canonical references are current.
- [x] 3.5 Split or reduce the largest shared test helper surface where scope is clear.

## 4. Validation, Sync, And Git

- [x] 4.1 Run focused unit/model checks for the touched runtime, packet, spec, card, and artifact surfaces.
- [x] 4.2 Run OpenSpec validation for the change and all specs.
- [x] 4.3 Start heavyweight meta/capability FlowGuard checks in background artifacts and inspect completion evidence.
- [x] 4.4 Sync the repository-owned installed FlowPilot skill and run local install sync/audit checks.
- [x] 4.5 Commit the final local git result without push, tag, release, or deploy.
