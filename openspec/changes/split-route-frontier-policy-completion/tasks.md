## 1. Plan And Split

- [x] 1.1 Confirm the latest MTA and synthetic-agent matrix identify only `flowpilot_router_route_frontier_policy_completion` as the remaining StructureMesh gap.
- [x] 1.2 Split route authority, legal-action, and node-completion helper blocks into focused child owner modules.
- [x] 1.3 Keep the original module as a public facade with the same exported symbol set and binding behavior.

## 2. Model/Test Alignment

- [x] 2.1 Register the completed split in the model-test-code diagnostic repair plan.
- [x] 2.2 Add focused facade/child parity tests and asset parseability coverage for the new child modules.
- [x] 2.3 Confirm the regenerated full diagnostic has zero `needs_structure_split` gaps and zero unresolved non-deferred gaps.

## 3. Verification And Sync

- [x] 3.1 Run syntax/import checks and focused runtime/diagnostic tests for the touched route-frontier surface.
- [x] 3.2 Run FlowGuard MTA, synthetic-agent matrix, ModelMesh, StructureMesh/TestMesh, and router facade split checks.
- [x] 3.3 Rebuild/check topology, update adoption evidence, sync the local installed FlowPilot skill, and validate OpenSpec.
