## 1. Model And Inventory

- [x] 1.1 Re-scan local lifecycle predicates, registry-derived maps, role-output binding fallbacks, and repeated prompt boundary text after the latest closure-kernel commit.
- [x] 1.2 Add or update focused FlowGuard model evidence for derived-view and prompt-boundary drift hazards.
- [x] 1.3 Confirm peer-agent dirty files are outside this change's write set before implementation.

## 2. Runtime Derived Views

- [x] 2.1 Route remaining card-return, current-work, scheduler, and PM-role blocker checks through the shared closure classifier where progress is blocked or cleared.
- [x] 2.2 Move control transaction compatibility maps from the Router facade into the control transaction registry owner while preserving facade exports.
- [x] 2.3 Move route action policy compatibility maps from the Router facade into the route frontier policy registry owner while preserving facade exports.
- [x] 2.4 Make remaining role-output/process binding fallbacks visible through registry-backed helpers or focused compatibility tests.

## 3. Prompt Boundary Policy

- [x] 3.1 Add shared prompt-policy assets or prompt-store ids for required return path, next-step authority, runtime context freshness, and role-scope boundaries.
- [x] 3.2 Add focused prompt/card validation that detects missing or contradictory common boundary policy across manifest cards.
- [x] 3.3 Update the smallest necessary set of card assets or rendering paths to use the shared prompt policy without changing role authority.

## 4. Focused Validation

- [x] 4.1 Run Python import/compile checks for touched runtime modules.
- [x] 4.2 Run focused tests for closure blockers, card returns, controller scheduler/current work, PM role work, registry export equality, output contracts, and prompt/card policy validation.
- [x] 4.3 Run OpenSpec status checks and record FlowGuard adoption evidence for this change.

## 5. Background Regressions And Sync

- [x] 5.1 Start heavyweight FlowGuard meta and capability regressions in `tmp/flowguard_background/` using the repository background-log artifact contract.
- [x] 5.2 Inspect final background artifacts, including stdout, stderr, combined log, exit code, meta JSON, latest update time, and proof-reuse status.
- [x] 5.3 Synchronize the installed local FlowPilot skill and verify installed content freshness against the repository.
- [x] 5.4 Re-check git status, stage only this change's files, and create the requested local git version without pushing, tagging, or publishing.
