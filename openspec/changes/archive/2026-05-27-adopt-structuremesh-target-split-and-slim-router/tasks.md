## 1. FlowGuard Target Structure

- [x] 1.1 Add `CodeStructureRecommendation` target structures to router facade and maintenance StructureMesh plans.
- [x] 1.2 Add `TestTargetSplitDerivation` target derivations to router facade and maintenance TestMesh plans.
- [x] 1.3 Rerun StructureMesh/TestMesh checks and keep known-bad hazards failing.

## 2. Router Catalog Split

- [x] 2.1 Extract the router protocol/catalog ownership band into a focused child module.
- [x] 2.2 Keep public router imports and CLI behavior compatible through the facade.
- [x] 2.3 Update StructureMesh ownership evidence for the extracted catalog owner.

## 3. Validation

- [x] 3.1 Run focused router/model alignment checks for the touched boundaries.
- [x] 3.2 Run router and release tier checks in hidden background mode and inspect final artifacts.
- [x] 3.3 Run current Meta and Capability FlowGuard regressions in hidden background mode and inspect final artifacts.

## 4. Install And Release

- [x] 4.1 Synchronize the installed local FlowPilot skill and verify freshness.
- [x] 4.2 Update release metadata to the next release version.
- [x] 4.3 Run public-release checks locally and keep GitHub push/release deferred.
- [x] 4.4 Commit locally; do not push or publish the GitHub Release.
