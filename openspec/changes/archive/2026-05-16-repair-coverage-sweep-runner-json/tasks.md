## 1. Contract

- [x] 1.1 Record the OpenSpec proposal/design/spec for JSON-capable coverage sweep runners.

## 2. Implementation

- [x] 2.1 Update the coverage sweep so every read-only runner with `--json` support is executed with `--json`.

## 3. Verification

- [x] 3.1 Add focused test coverage for runners that support both `--json` and `--json-out`.
- [x] 3.2 Run focused maintenance tests and coverage sweep verification.
