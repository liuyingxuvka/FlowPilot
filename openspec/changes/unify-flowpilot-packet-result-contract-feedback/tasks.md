## 1. OpenSpec And FlowGuard Record

- [x] 1.1 Record this focused current-contract follow-up change.
- [x] 1.2 Sync the local FlowGuard project record with the installed package version.
- [x] 1.3 Extend this change from packet-result feedback into the full current
      contract chain: high-standard contract, route projection,
      node-context-package, PM disposition, final ledger, and terminal backward
      replay.

## 2. Runtime Contract Bridge

- [x] 2.1 Extend packet-result contract rows for Reviewer and FlowGuard operator packet families to require the existing rich report fields.
- [x] 2.2 Make runtime mechanical validation understand current nested field paths and explicit arrays for those packet families.
- [x] 2.3 Keep runtime semantic outcome parsing current-contract only, with no legacy alias fallback.
- [x] 2.4 Require high-standard contract rows to carry user-intent proof
      metadata: source, evidence rule, report-only policy, and optional
      forbidden/done/quality/waiver fields.
- [x] 2.5 Require node context packages to carry hard requirement projection,
      low-quality risks, semantic downgrade risks, work-packet projection, and
      test-obligation matrix rows.
- [x] 2.6 Require PM dispositions to absorb Reviewer and FlowGuard reports,
      requirement coverage, residual risk disposition, and semantic downgrade
      disposition before accepting a node.

## 3. Fake AI And Prompt/Contract Alignment

- [x] 3.1 Update fake AI success bodies so Reviewer and FlowGuard packets submit the declared current rich report fields.
- [x] 3.2 Align role output contract indexes with the required `pm_visible_summary` fields already named by the role cards.
- [x] 3.3 Add negative fake AI/runtime cases for generic minimal Reviewer and FlowGuard success bodies.
- [x] 3.4 Add negative fake AI/runtime cases for empty/generic rich fields,
      missing PM absorption, forbidden output, stale report, and hard
      requirement coverage without direct proof.

## 4. FlowGuard Model Coverage

- [x] 4.1 Extend FieldContract or source-alignment checks so role report body fields are part of packet-result family coverage.
- [x] 4.2 Add model/source checks that fail when fake AI or runtime success can bypass the role report contract.
- [x] 4.3 Extend model/source checks for the high-standard contract ->
      route-node -> node-context-package -> role-report -> PM-disposition ->
      final-ledger chain.

## 5. Final Closure And Deliverable Checks

- [x] 5.1 Extend route `deliverable_checks` in the existing surface with JSON
      path, JSON equality, text contains/forbids, path/glob absence, and
      freshness checks.
- [x] 5.2 Make final requirement evidence rows require direct current evidence
      or explicit waiver for hard requirements instead of accepted-node
      existence alone.
- [x] 5.3 Require terminal backward replay before high-standard final
      preflight can allow completion.

## 6. Validation And Sync

- [x] 6.1 Run targeted runtime, fake AI, role output, and OpenSpec tests.
- [x] 6.2 Run affected FlowGuard model checks and topology build/check.
- [x] 6.3 Sync the local FlowPilot install and run install/audit checks.
- [x] 6.4 Commit the final local git version without reverting peer-agent changes.
