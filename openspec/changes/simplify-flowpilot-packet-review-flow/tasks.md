## 1. OpenSpec And FlowGuard Model Boundary

- [x] 1.1 Validate the OpenSpec change strictly after proposal, design, specs, and task artifacts exist.
- [x] 1.2 Extend or update the focused FlowGuard packet/review-flow model obligations so known-bad direct-to-Reviewer and missing-formal-package paths fail.
- [x] 1.3 Update Meta and Capability PM-review-release invariants so absorbed PM disposition with formal gate package release satisfies the PM release gate.

## 2. Runtime Flow Simplification

- [x] 2.1 Add recipient-neutral result relay checking while keeping the legacy reviewer-named relay check as a compatibility alias.
- [x] 2.2 Update PM package-result disposition and formal gate package checks so Reviewer release evidence is read from the absorbed disposition record.
- [x] 2.3 Mark reviewer-dispatch cards/events as legacy-only for new PM-authored worker/officer packet flows without deleting compatibility paths.
- [x] 2.4 Ensure material, research, current-node, and PM role-work next-action text routes PM-bound results to PM disposition and Reviewer-bound results to Reviewer delivery.

## 3. Cards, Docs, And User-Facing Flow Clarity

- [x] 3.1 Update runtime phase cards so the visible flow is PM issues package, Router direct-dispatches, result returns to PM, PM releases formal package, Reviewer reviews package.
- [x] 3.2 Update Reviewer cards so Router mechanical proof is cited only for envelope/hash/ledger facts and Reviewer still owns quality/source/fact checks.
- [x] 3.3 Update stale protocol and planning docs that still describe raw Worker results going directly to Reviewer or Reviewer pre-dispatch approval as current flow.

## 4. Verification

- [x] 4.1 Run focused packet runtime, router runtime, control gate, and control-plane tests for touched behavior.
- [x] 4.2 Start Meta and Capability FlowGuard regressions in the required background artifact directory and inspect exit/meta artifacts before claiming completion.
- [x] 4.3 Run install checks, local install sync, and local install audit/check after source changes are complete.

## 5. Local Version And Coordination

- [x] 5.1 Recheck git status before staging and avoid unrelated peer-agent files.
- [x] 5.2 Sync repo-owned FlowPilot assets to the installed local skill and verify source freshness.
- [x] 5.3 Create the requested local git version only after tests and install freshness evidence are current.
