## 1. Scope And Guardrails

- [x] 1.1 Validate OpenSpec proposal/spec artifacts and record the clean-language target vocabulary.
- [x] 1.2 Run FlowGuard existing-model and development-process preflight to map affected prompt, template, model, test, and install surfaces.
- [x] 1.3 Preserve pre-existing unrelated dirty files and define the staged commit boundary.

## 2. Active Prompt And Runtime Entrypoint Cleanup

- [x] 2.1 Update `skills/flowpilot/SKILL.md` so formal FlowPilot startup and Controller duty mention only the new runtime path and current Router/ledger authority.
- [x] 2.2 Update runtime kit shared prompt-policy snippets and cards to remove current-instruction references to old router repair, old crew, fixed crew, full role binding recycle, sidecar roles, and unsupported historical paths.
- [x] 2.3 Update runtime kit manifest/card identifiers where current authority still names role binding recovery, or add a migration-safe alias only when required by tests and hidden from role-facing wording.

## 3. Template And Runtime Surface Cleanup

- [x] 3.1 Rewrite `templates/flowpilot` current-state templates from crew/background/sidecar role vocabulary to role-binding/requested-responsibility vocabulary.
- [x] 3.2 Update runtime CLI/state/source files that expose `runtime_role_assistances`, `crew`, `sidecar role`, `required_role_binding`, or `FlowPilot runtime` as current authority.
- [x] 3.3 Rephrase unsupported historical-path errors and checks to say unsupported input/channel instead of unsupported historical/unsupported unsupported historical workflow.

## 4. Model, Test, And Validation Migration

- [x] 4.1 Update FlowGuard meta/capability model terminology and startup/resume obligations from fixed role binding coverage to requested role-binding coverage.
- [x] 4.2 Update focused tests, install checks, maintenance registry, and prompt-boundary scans for the clean-language contract.
- [x] 4.3 Regenerate affected result artifacts only from current checks, preserving unrelated peer/user changes unless intentionally superseded by rerun output.

## 5. Verification, Install Sync, And Git

- [x] 5.1 Run OpenSpec validation, focused prompt/template scans, and targeted unit/model checks.
- [x] 5.2 Run heavyweight meta/capability regressions through the background log contract and inspect completion artifacts before claiming pass.
- [x] 5.3 Rebuild and check FlowGuard project topology after model/check/prompt changes.
- [x] 5.4 Sync the local installed FlowPilot skill and run install freshness/audit checks.
- [x] 5.5 Run KB postflight, stage only this change set, and create a local git commit.
