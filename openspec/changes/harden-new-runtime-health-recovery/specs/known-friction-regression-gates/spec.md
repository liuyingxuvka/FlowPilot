## MODIFIED Requirements

### Requirement: New Runtime Health Regression Family

Known-friction regression coverage SHALL include the `run-20260531-210441`
health family: stale accepted-packet leases, broad sealed-body projection,
under-specified recovery duty, node context package schema drift, unstable
evidence summaries, and stale terminal pointer display.

#### Scenario: Focused friction family is covered

- **GIVEN** the new-runtime focused regression runner executes
- **WHEN** it reports the current health family
- **THEN** every listed friction source has executable or static evidence
- **AND** missing coverage fails the focused check
