## 1. Model And Evidence Grounding

- [x] 1.1 Record model-miss evidence for the current repeated `open_startup_intake` run and same-class no-delta retry cases in the design/evidence trail.
- [x] 1.2 Inventory existing packet/result/report contract families and select broad required rejection/liveness cell ids for routine confidence, including missing fields, missing bodies, missing paths, missing hashes, wrong owners, wrong ids, unsupported enums, stale evidence, prose-only critical content, contradictory pass/blocker states, same-payload retry, same-action retry, and corrected retry.
- [x] 1.3 Add a focused FlowGuard rejection/liveness model with hazards for vague rejection feedback, missing repair target, same-payload retry, stuck detection without absorption, and mesh-green live projection over a repeated action.
- [x] 1.4 Add or update a runner and JSON result artifact for the focused rejection/liveness model.

## 2. TestMesh And Coverage Matrix

- [x] 2.1 Add a parent TestMesh or coverage report that maps required rejection/liveness cell ids to child evidence owners.
- [x] 2.2 Extend synthetic-agent coverage so required rejection/liveness cells have branch kinds, evidence owner ids, confidence boundaries, and missing-owner failure behavior.
- [x] 2.3 Add model-test alignment rows binding rejection/liveness obligations to runtime/model-mesh/test evidence.
- [x] 2.4 Keep release-only or non-live AI semantic quality boundaries visible instead of treating synthetic evidence as release confidence.

## 3. Runtime And Live Projection Repair

- [x] 3.1 Add or adjust runtime/lifecycle guard behavior so repeated same action and same observed event count above threshold remains a stable blocker until new progress evidence exists.
- [x] 3.2 Add or adjust model mesh/live projection behavior so current-run repeated-action metadata cannot return `mesh_green_can_continue`.
- [x] 3.3 Add precise rejection feedback checks for malformed or insufficient packet/result/report outputs that currently allow vague reissue.
- [x] 3.4 Preserve legitimate user-required or role-dispatch waits only when current interactive wait or new-event evidence is present.

## 4. Fake AI, Historical Replay, And Regression Tests

- [x] 4.1 Add broad fake AI rehearsal rows for malformed output followed by same-payload/no-delta retry and corrected retry across every practical current contract family.
- [x] 4.2 Add historical replay coverage for the current repeated startup-intake ledger shape.
- [x] 4.3 Add focused runtime tests for stuck absorption after `control_plane_stuck`.
- [x] 4.4 Add focused projection/model-mesh tests that fail when a repeated current-run action is reported green.
- [x] 4.5 Add negative tests for vague rejection feedback and same-subject no-delta continuation.

## 5. Validation, Sync, And Records

- [x] 5.1 Run the new focused FlowGuard model checks and inspect the result artifact.
- [x] 5.2 Run affected runtime, lifecycle guard, fake AI, historical replay, synthetic coverage, model mesh, and model-test alignment tests.
- [x] 5.3 Rebuild and check FlowGuard project topology after model/test/runtime changes.
- [x] 5.4 Run install checks and local install sync audit, then sync the installed FlowPilot skill if source changes require it.
- [x] 5.5 Update FlowGuard adoption records with commands, findings, skipped evidence, and scoped confidence boundaries.
- [x] 5.6 Run KB postflight and record any reusable model/test/process lesson.
- [x] 5.7 Commit the scoped change to local git after validation.
