## 1. Model And Miss Grounding

- [x] 1.1 Preserve the observed miss as a generalized FlowGuard bad-case family: structured PM decision with hostile prose, internal action exposed as foreground work, stopped/repair split-brain, and status-read mutation.
- [x] 1.2 Add or update a focused FlowGuard/check runner for new-runtime control-plane duty classification and invariants.
- [x] 1.3 Register ordinary regression evidence targets for parser, action folding, status read-only, blocker lifecycle consistency, and public fake rehearsal.

## 2. Runtime Implementation

- [x] 2.1 Add new-runtime action classification for router-internal, role-dispatch, role-wait, recovery, user-required, terminal, and controller-external boundaries.
- [x] 2.2 Add a bounded `run_until_wait` fold loop and public CLI command for the new runtime.
- [x] 2.3 Implement allowlisted internal mechanical actions for packet/materialization steps currently handled only by fake direct helper calls.
- [x] 2.4 Harden PM repair decision parsing to require structured decision fields and reject missing/ambiguous decision payloads.
- [x] 2.5 Enforce blocker lifecycle consistency so stopped blockers cannot continue repair and same-node repair cannot mark packets stopped.
- [x] 2.6 Make `status` read-only for guard/event/history mutation while keeping patrol and stateful commands as refresh points.

## 3. Rehearsal And Tests

- [x] 3.1 Add focused unit tests for structured PM repair parsing and missing-decision rejection.
- [x] 3.2 Add focused unit tests for new-runtime `run_until_wait` folding to role boundaries.
- [x] 3.3 Add focused unit tests for status read-only behavior and patrol refresh behavior.
- [x] 3.4 Add focused unit tests for stopped-blocker versus same-node-repair lifecycle invariants.
- [x] 3.5 Update fake E2E rehearsal to use the public fold path and cover adversarial PM repair prose.

## 4. Validation And Sync

- [x] 4.1 Run OpenSpec status/validation for this change.
- [x] 4.2 Run real FlowGuard package audit and focused model/check runner.
- [x] 4.3 Run targeted pytest/unittest suites for new-runtime runtime, complete-system runtime, and fake rehearsal.
- [x] 4.4 Start broader meta/capability or tiered regressions in background artifacts where practical and inspect completion evidence before counting them.
- [x] 4.5 Sync the repository FlowPilot skill to the local installed skill and run install/audit checks.
- [x] 4.6 Inspect git status, preserve unrelated dirty work, record FlowGuard adoption and KB postflight evidence, and report the final confidence boundary.
