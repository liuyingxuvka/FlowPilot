## 1. Model And Contract

- [x] 1.1 Add a focused FlowGuard material artifact map model and runner covering map refresh, sealed-body exclusion, packet-authorized worker reads, reviewer source refs, route-memory linkage, and final-ledger stale/unresolved rejection.
- [x] 1.2 Add material artifact map schema/contract documentation in `docs/schema.md` and runtime-kit prompts/contracts.

## 2. Core Implementation

- [x] 2.1 Add a material artifact map helper that derives map entries from existing run artifacts without reading sealed packet/result bodies.
- [x] 2.2 Refresh or reference the map from material scan packet creation, PM package disposition, material sufficiency, research package/result absorption, and PM material understanding.
- [x] 2.3 Link the material map from route memory and final route-wide ledger source scans.

## 3. Role Guidance And Validation

- [x] 3.1 Update PM, worker, reviewer, and prior-path context cards so roles use material-map refs only within existing packet/runtime boundaries.
- [x] 3.2 Enforce reviewer material sufficiency checked-source requirements and PM formal package review refs.

## 4. Tests, Sync, And Evidence

- [x] 4.1 Add targeted router/runtime tests for map generation, sealed-body exclusion, worker allowed reads, reviewer checked-source blocking, and route-memory/final-ledger refs.
- [x] 4.2 Run focused FlowGuard and router tests, then start long model/router regressions in the documented background log root.
- [x] 4.3 Sync the local installed FlowPilot skill and verify source freshness.
- [x] 4.4 Review git status and commit the completed local source/spec/test/install sync.
