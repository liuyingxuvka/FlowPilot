## 1. Prompt Contract

- [x] 1.1 Strengthen the Controller role-card user-facing report guidance.
- [x] 1.2 Strengthen the generated Controller table prompt with the same
  plain-language default.

## 2. Validation

- [x] 2.1 Add install checks that require the strengthened guidance in both
  prompt locations.
- [x] 2.2 Extend focused FlowGuard coverage for table-prompt user-language
  guidance.
- [x] 2.3 Run targeted validations while skipping the heavy Meta and Capability
  simulations.

## 3. Sync

- [x] 3.1 Sync the installed local FlowPilot skill after validation.
- [x] 3.2 Review local git state so parallel AI work is preserved in the final
  local commit.
